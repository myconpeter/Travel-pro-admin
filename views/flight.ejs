<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Design by foolishdeveloper.com -->
    <title>Travel-Pro</title>
 
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
    <!--Stylesheet-->
    <style media="screen">
      *,
*:before,
*:after{
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}
body{
    background-color: whitesmoke;
}
.background{
    width: 430px;
    height: 520px;
    position: absolute;
    transform: translate(-50%,-50%);
    left: 50%;
    top: 50%;
}
.background .shape{
    height: 200px;
    width: 200px;
    position: absolute;
    border-radius: 50%;
}


form{
    height: 520px;
    width: 400px;
    background-color: black;
    position: absolute;
    transform: translate(-50%,-50%);
    top: 50%;
    left: 50%;
    border-radius: 10px;
    backdrop-filter: blur(10px);
    padding: 50px 35px;
}
form *{
    font-family: 'Poppins',sans-serif;
    color: #ffffff;
    letter-spacing: 0.5px;
    outline: none;
    border: none;
}
form h3{
    font-size: 32px;
    font-weight: 500;
    line-height: 42px;
    text-align: center;
}


input{
    display: block;
    height: 50px;
    width: 100%;
    background-color: rgba(255,255,255,0.07);
    border-radius: 3px;
    padding: 0 10px;
    margin-top: 8px;
    font-size: 14px;
    font-weight: 300;
}
::placeholder{
    color: #e5e5e5;
}
button{
    margin-top: 50px;
    width: 100%;
    background-color: #ffffff;
    color: #080710;
    padding: 15px 0;
    font-size: 18px;
    font-weight: 600;
    border-radius: 5px;
    cursor: pointer;
}


    </style>
</head>
<body>
    
    <form action="/createPlane" method="POST">
        <%- include ('./partials/messages') %>
        <h3>Flight Ticket</h3>
        <p style="text-align: center;">All MUST BE IN UPPERCASE LETTER</p>

        <input type="text" placeholder="Full Name e.g JOHN PETER" name="name" id="name" value="<%= typeof name != 'undefined' ? name : '' %>">
        <input type="text" placeholder="Plane Seat e.g 0413" name="seat" id="seat" value="<%= typeof seat != 'undefined' ? seat : '' %>">
        <div style="display: flex;">
        <input type="text" placeholder="Gate e.g 023" name="gate" id="gate" style="margin-right: 5px;" value="<%= typeof gate != 'undefined' ? gate : '' %>" >
        <input type="text" placeholder="No e.g BA 0254" name="flightNo" id="flightNo" value="<%= typeof flightNo != 'undefined' ? flightNo : '' %>">
        </div>

        <div style="display: flex;">
            <select id="class"  name="planeClass" style="margin-right: 5px; background-color: #080710;">
                <option value="">Select Ticket Class</option>
                <option value="First Class">First Class</option>
                <option value="Business Class">Business Class</option>
                <option value="Economy Class">Economy Class</option>
                <!-- Add more options for additional classes if needed -->
            </select>
            
            <input type="text" placeholder="time e.g 14:44 " id="time" name="time" value="<%= typeof time != 'undefined' ? time : '' %>">
        </div>
        
        <div style="display: flex;">
            <input type="text" placeholder="From e.g ALASKA " id="from" name="from" style="margin-right: 5px;" value="<%= typeof from != 'undefined' ? from : '' %>" >
            <input type="text" placeholder="to e.g LONDON " id="to" name="to" value="<%= typeof to != 'undefined' ? to : '' %>">
        </div>
       
        <input type="text" placeholder="Departure day e.g 12/SEPT/2024" name="departure" id="dep" value="<%= typeof departure != 'undefined' ? departure : '' %>">


        

        <button type="submit">Create Ticket</button>
       
    </form>
</body>
</html>
